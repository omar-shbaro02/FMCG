# Contributing

Work through `PROJECT_TASKS.md` in order. Preserve the frozen product canon and
stop at human review gates. Each change must include tests appropriate to its
risk, changed-file and assumption notes in `TASK_LOG.md`, and confirmation that
the application still performs no autonomous commercial action.

Use Conventional Commit-style subjects, run `make check`, and never add real
client or personal data to fixtures.
